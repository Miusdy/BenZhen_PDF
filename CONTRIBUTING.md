# 贡献指南

感谢参与本真 PDF。请保持转换行为确定、可追溯且离线。

1. 从主题分支提交小而清晰的变更。
2. 新功能必须附带测试；涉及内容选择规则时同时覆盖关键数字冲突。
3. 不提交真实用户 PDF、OCR 文本、密码、日志、缓存或未确认授权的字体/OCR 模型。
4. 不引入远程脚本、CDN、分析或追踪服务。
5. 执行 `pytest`、`pnpm test` 与 `pnpm build`。
6. 将未实机验证的平台明确写入 PR 说明。
7. 单个文本文件必须小于 100 MB；二进制依赖请使用发行制品或合规的下载构建步骤。

