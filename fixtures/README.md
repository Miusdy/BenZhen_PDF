# 测试夹具

本目录中的 PDF 由 `build/scripts/generate_fixtures.py` 生成，不含真实个人或业务信息：

- `中文 路径-文字与表格.pdf`：中文、关键数字、表格、页眉页脚、双栏与空格路径；
- `scanned-invoice.pdf`：只有整页图片的扫描件；
- `mixed-text-scan.pdf`：文字页与扫描页混合；
- `blank-page.pdf`：空白页；
- `encrypted.pdf`：密码为测试专用的 `test-password`。

二进制夹具可以重新生成，不应用于产品样例或性能基准。

